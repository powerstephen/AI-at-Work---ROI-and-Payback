# Brainster “AI at Work” — ROI & Payback Builder

A 4-step ROI wizard (Next.js + Tailwind + TypeScript) modeled on the Factorial HR calculator.

## Quick start
```bash
pnpm i         # or npm i / yarn
pnpm dev       # http://localhost:3000
